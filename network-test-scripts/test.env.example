# ─────────────────────────────────────────────────────────────────────────────
# test.env.example — shared configuration for all network-test-scripts
#
# HOW TO USE:
#   1. Copy this file to test.env in the same directory:
#        cp test.env.example test.env
#   2. Fill in your actual values below.
#   3. Every script in this repo will automatically source test.env on startup,
#      so you only need to set these values once.
#   4. CLI arguments still take priority — test.env values become the defaults.
#
# test.env is listed in .gitignore so your IPs won't be committed.
# ─────────────────────────────────────────────────────────────────────────────

# ── Host IPs ─────────────────────────────────────────────────────────────────

# Private (or public) IP of the RHEL8 EC2 (nc receiver / diagnostics source)
RHEL8_IP="10.0.0.10"

# Private (or public) IP of the RHEL9 EC2 (Podman host / nc server)
RHEL9_IP="10.0.0.20"

# ── Port ─────────────────────────────────────────────────────────────────────

# Port the Podman container publishes on the RHEL9 host
TEST_PORT="21240"

# ── Container ────────────────────────────────────────────────────────────────

# Name of the Podman container running on the RHEL9 host
CONTAINER_NAME="nc-test-server"

# Container image (used by podman_server_setup.sh)
CONTAINER_IMAGE="registry.access.redhat.com/ubi9/ubi-minimal"

# Port nc listens on INSIDE the container (usually the same as TEST_PORT)
CONTAINER_PORT="${TEST_PORT}"

# ── Timeouts ─────────────────────────────────────────────────────────────────

# Per-attempt connection timeout in seconds (nc, curl, python3, etc.)
TIMEOUT_SECS="5"

# Duration in seconds for tcpdump_capture.sh
CAPTURE_DURATION="30"
